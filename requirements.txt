# Dependencies for segment_kmeans.py
# Core (required):
numpy>=1.24
pandas>=1.5
scipy>=1.10
scikit-learn>=1.2
# Optional (nicer output / more formats; the tool degrades gracefully without them):
tabulate>=0.9        # Markdown tables in the report
matplotlib>=3.6      # the diagnostics figure
openpyxl>=3.0        # read .xlsx Excel survey exports (CSV needs nothing extra)
# Development (to run the test suite):
pytest>=7.0
#
# Verified working with: numpy 2.0.2, pandas 2.3.3, scipy 1.13.1, scikit-learn 1.6.1
# Install: pip install -r requirements-segmentation.txt
